# Default values for helm-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: avinashdere99/tomcat:tag
  pullPolicy: Always

service:
  type: NodePort
  port: 80
  nodePort: 30890

